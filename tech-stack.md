# Wonderful 技术栈 & 成本方案（从零开始）

> 前提：什么都没有，没有服务器、没有域名、没有账号。
> 目标：花最少的钱，最快跑起来，能承载从1到10000用户的增长。

---

## 一、技术栈总览

```
┌─────────────────────────────────────────────────┐
│                    用户触达层                      │
│                                                  │
│   微信小程序 (主)  +  PWA Web (备)                 │
│   WXML/WXSS/JS     Vue 3 + Vite                  │
└──────────────────────┬──────────────────────────┘
                       │ HTTPS
┌──────────────────────▼──────────────────────────┐
│                    后端服务                        │
│                                                  │
│   Python 3.12 + FastAPI                           │
│   (部署在阿里云轻量服务器 或 微信云开发)              │
└───────┬──────────┬──────────┬───────────────────┘
        │          │          │
   ┌────▼───┐ ┌───▼────┐ ┌──▼──────────┐
   │ SQLite │ │ AI引擎  │ │ 定时任务     │
   │ 数据库  │ │ LLM API │ │ APScheduler │
   └────────┘ └────────┘ └─────────────┘
                  │
        ┌────────┴────────┐
        │  DeepSeek (主)   │
        │  GLM-4-Flash(备) │
        └─────────────────┘
```

---

## 二、每一层的选型和理由

### 1. 前端：微信小程序

**为什么选小程序而不是 App：**

| 方案 | 开发成本 | 用户获取 | 通知能力 | 适合阶段 |
|------|---------|---------|---------|---------|
| 微信小程序 | 低 | 零安装，扫码即用 | 订阅消息 | MVP首选 |
| Flutter/RN App | 高 | 需下载，需上架 | 推送强 | 成熟期 |
| PWA Web | 中 | 一个链接 | 弱(国内) | 备选/海外 |

小程序的核心优势：**用户不需要下载任何东西**，分享一个小程序卡片到微信群就能传播。

**个人开发者注册小程序：**
- 注册费：免费
- 年度认证：**30元/年**（2023年起必须，不认证会被下架）
- 个人可发布的类目：工具、教育、生活服务 ← **你的产品属于"工具"类，没问题**
- 限制：个人不能接入微信支付（金币系统只能是虚拟积分，不涉及真实交易就没问题）

**订阅消息（晨启/晚审通知）：**
- 用户必须**主动点击授权**才能接收
- 如果用户勾选了"总是允许"，后续可无限发送该模板消息
- 策略：首次使用时引导用户授权晨启+晚审两个消息模板，勾选"总是允许"

### 2. 后端：Python + FastAPI

```
为什么选 Python + FastAPI：
├── 你已经接触 Python（A股项目也是Python）
├── FastAPI 是当前最快的 Python Web 框架
├── 异步支持好，AI API 调用不会阻塞
├── 生态丰富，AI/LLM 库都是 Python 优先
└── 部署简单，一个文件就能跑
```

核心依赖：
```
fastapi          # Web 框架
uvicorn          # ASGI 服务器
httpx            # 异步 HTTP 客户端（调 AI API）
apscheduler      # 定时任务（晨启/晚审）
pydantic         # 数据校验
sqlite3          # 数据库（Python内置）
```

### 3. 数据库：SQLite（起步） → PostgreSQL（增长后）

**为什么 SQLite 起步最合适：**
- Python 内置，零安装零配置
- 文件型数据库，备份就是复制一个文件
- 100用户绰绰有余，1000用户也能撑住
- 不需要额外花钱

**数据表设计预览：**
```sql
-- 用户表
users (id, openid, nickname, created_at, coins, streak_days, settings_json)

-- 任务卡片
cards (id, user_id, title, description, priority, status,
       category, due_date, parent_card_id, estimated_minutes,
       coin_reward, created_at, completed_at)

-- 每日记录
daily_logs (id, user_id, date, morning_message, evening_summary,
            completed_cards, uncompleted_cards, coins_earned)

-- 思维转换库
mindset_shifts (id, user_id, original_thought, reframed_thought,
                likes_count, is_public, created_at)

-- 奖励商城
rewards (id, user_id, title, coin_cost, times_redeemed)

-- AI 对话记录（用于"越用越懂你"）
ai_memory (id, user_id, insight_type, content, confidence, created_at)
```

### 4. AI 引擎：DeepSeek（主） + 智谱GLM-4-Flash（备/免费）

这是最关键的选型，直接影响成本和体验。

---

## 三、AI 模型选型 & Token 成本详算

### 中文 Token 换算

不同模型对中文的 Token 效率不同：

| 模型 | 1个汉字 ≈ Token数 | 100字 ≈ |
|------|------------------|---------|
| Claude | ~1.0-1.5 tokens | ~100-150 tokens |
| GPT-4 | ~1.0-1.5 tokens | ~100-150 tokens |
| DeepSeek | ~0.6-0.75 tokens | ~60-75 tokens |
| 通义千问/GLM | ~0.6-0.75 tokens | ~60-75 tokens |

**国产模型处理中文效率高约 50%，同样的中文内容花更少的 token。**

### 你的产品每次 AI 调用量估算

| 场景 | 输入(系统提示+用户数据) | 输出(AI生成) | 频率 |
|------|----------------------|-------------|------|
| 晨启对话 | ~300 tokens | ~150 tokens | 每天1次 |
| 晚审对话 | ~400 tokens | ~250 tokens | 每天1次 |
| 思维转换 | ~300 tokens | ~100 tokens | 每天~1次 |
| **日合计/人** | **~1000 tokens** | **~500 tokens** | **3次** |
| **月合计/人** | **~30,000 tokens** | **~15,000 tokens** | **~90次** |

### 各模型价格对比（每百万 Token，人民币）

| 模型 | 输入价 | 输出价 | 质量 | 推荐度 |
|------|--------|--------|------|--------|
| **智谱 GLM-4-Flash** | **免费** | **免费** | 够用 | ⭐⭐⭐⭐ 起步首选 |
| **百度 ERNIE-Speed** | **免费** | **免费** | 够用 | ⭐⭐⭐ 备选 |
| **DeepSeek V3** | ¥1(缓存¥0.1) | ¥2 | 优秀 | ⭐⭐⭐⭐⭐ 性价比之王 |
| 智谱 GLM-4.5 | ¥0.8 | ¥2 | 很好 | ⭐⭐⭐⭐ |
| 通义千问 Qwen-Turbo | ¥2 | ¥6 | 好 | ⭐⭐⭐ |
| Moonshot Kimi | ¥1.5 | ¥15 | 好 | ⭐⭐ 输出贵 |
| Claude Haiku 4.5 | ¥7.3 | ¥36.5 | 很好 | ⭐ 太贵 |
| Claude Sonnet 4.6 | ¥22 | ¥110 | 顶级 | 不适合 |

### 月成本测算（人民币）

#### 方案A：GLM-4-Flash（完全免费）

| 用户数 | 月 AI 成本 |
|--------|-----------|
| 100 | **¥0** |
| 1,000 | **¥0** |
| 10,000 | **¥0** |

质量评估：能生成通顺的中文，做晨启/晚审/思维转换**够用**。
缺点：创意性和个性化不如 DeepSeek。

#### 方案B：DeepSeek V3（推荐）

| 用户数 | 月输入Token | 月输出Token | 月成本 |
|--------|-----------|-----------|--------|
| 100 | 3M | 1.5M | **¥6** |
| 1,000 | 30M | 15M | **¥60** |
| 10,000 | 300M | 150M | **¥600** |

如果用好缓存（系统提示复用），输入成本再降 90%：

| 用户数 | 缓存后月成本 |
|--------|------------|
| 100 | **¥3.3** |
| 1,000 | **¥33** |
| 10,000 | **¥330** |

#### 推荐方案：分层混合

```
免费层:  GLM-4-Flash  → 晨启消息、晚审基础版（固定模板+少量个性化）
付费层:  DeepSeek V3  → 思维转换（需要创意）、深度个性化建议
                        "越用越懂你"的洞察分析

这样 70% 的调用走免费，30% 走 DeepSeek
1000用户月成本: ¥0×70% + ¥60×30% = ¥18
```

---

## 四、服务器 & 基础设施成本

### 服务器

| 阶段 | 方案 | 配置 | 年费(¥) | 月均(¥) |
|------|------|------|---------|---------|
| **开发期** | 微信云开发免费环境 | 未发布前免费 | **0** | **0** |
| **上线MVP** | 阿里云轻量服务器 | 2核2G 200M峰值带宽 | **38-68** | **3-6** |
| **增长期** | 阿里云轻量服务器 | 2核4G | **199-298** | **17-25** |

**最省方案**：先用微信云开发（开发阶段免费），发布后切换到阿里云轻量（38元/年）。

### 域名 & 备案

| 项目 | 费用 | 说明 |
|------|------|------|
| .cn 域名 | 首年¥1, 续费¥35/年 | 阿里云新用户优惠 |
| ICP备案 | **免费** | 通过阿里云提交，2-4周 |
| SSL证书 | **免费** | Let's Encrypt 自动续期 |

**注意**：微信小程序2023年9月起必须 ICP 备案才能发布。
如果用**微信云开发模式**，不需要自己买域名和服务器，但小程序本身仍需备案。

### 微信云开发费用（如果选这条路）

| 项目 | 费用 |
|------|------|
| 开发阶段（未发布） | **免费** |
| 发布后基础套餐 | **¥19.9/月** (¥239/年) |
| 包含额度 | 2GB存储, 5GB CDN, 1GB数据库, 10万次云函数 |

---

## 五、总成本汇总

### 方案一：最省钱路线（微信云开发 + GLM免费AI）

适合：验证想法阶段，0-500用户

| 项目 | 年费(¥) |
|------|---------|
| 小程序认证 | 30 |
| 微信云开发 | 239 |
| AI (GLM-4-Flash) | 0 |
| 域名 (.cn) | 1 (首年) |
| ICP备案 | 0 |
| **总计** | **¥270/年 ≈ ¥23/月** |

### 方案二：推荐路线（自有服务器 + 混合AI）

适合：认真做产品，500-5000用户

| 项目 | 年费(¥) |
|------|---------|
| 小程序认证 | 30 |
| 阿里云服务器 2C2G | 68 |
| AI - GLM-4-Flash (70%) | 0 |
| AI - DeepSeek V3 (30%) | ~400 (1000用户) |
| 域名 (.cn) | 35 |
| ICP备案 / SSL | 0 |
| **总计** | **~¥533/年 ≈ ¥44/月** |

### 方案三：增长路线（5000-10000用户）

| 项目 | 年费(¥) |
|------|---------|
| 小程序认证 | 30 |
| 阿里云服务器 2C4G | 298 |
| AI - DeepSeek V3 (全量) | ~4,000 |
| 域名 (.cn) | 35 |
| **总计** | **~¥4,363/年 ≈ ¥364/月** |

### 成本对比图

```
月成本(¥)
  400 │                              ┌───┐
      │                              │   │ 方案三
  300 │                              │   │ (10000用户)
      │                              │   │
  200 │                              │   │
      │                              │   │
  100 │                    ┌───┐     │   │
      │                    │   │ 方案二│   │
   50 │          ┌───┐     │   │     │   │
      │  ┌───┐   │   │     │   │     │   │
   23 │  │   │ 方案一│   │     │   │     │   │
      └──┴───┴───┴───┴─────┴───┴─────┴───┴──
       0-100   100-500  500-5K   5K-10K  用户数
```

---

## 六、从零开始的操作清单

### 第一步：注册账号（第1天）

```
□ 注册阿里云账号 (aliyun.com) → 实名认证
□ 注册微信公众平台 (mp.weixin.qq.com) → 选择"小程序" → 个人注册
□ 完成小程序认证 (¥30)
□ 注册智谱AI开放平台 (bigmodel.cn) → 获取 API Key (GLM-4-Flash 免费)
□ 注册 DeepSeek 开放平台 (platform.deepseek.com) → 获取 API Key (送500万token)
□ 注册 GitHub 账号 (代码托管)
```

### 第二步：购买资源（第1天）

```
□ 阿里云购买 .cn 域名 (¥1)
□ 阿里云购买轻量服务器 2C2G (¥38-68/年)
  或者先用微信云开发免费环境
□ 提交 ICP 备案 (免费，但要等2-4周)
```

### 第三步：开发环境搭建（第2天）

```
□ 安装微信开发者工具
□ 安装 Python 3.12 + VS Code
□ 创建项目仓库
□ 搭建 FastAPI 后端骨架
□ 测试 AI API 调通（GLM-4-Flash + DeepSeek）
```

### 第四步：MVP 开发（第1-2周）

```
□ 后端: 用户系统 + 任务CRUD + AI对话接口 + 定时任务
□ 小程序: 首页对话界面 + 任务卡片 + 金币显示
□ AI: 晨启prompt + 晚审prompt + 思维转换prompt
□ 通知: 订阅消息模板申请 + 推送逻辑
```

### 第五步：测试上线（第3周）

```
□ ICP备案通过（如果还没通过就等）
□ 小程序提交审核
□ 自己先用一周，调优 AI prompt
□ 邀请3-5个朋友内测
```

---

## 七、关键技术细节

### AI Prompt 设计（产品灵魂）

晨启 prompt 示例：
```
你是 Wonderful，一个温暖的 AI 朋友。
你的任务是帮用户规划今天最值得做的事。

用户信息：
- 昵称：{nickname}
- 待办任务：{tasks_json}
- 昨日完成情况：{yesterday_summary}
- 连续打卡天数：{streak_days}
- AI 对用户的了解：{ai_memory}

要求：
1. 从待办中挑出今天最值得做的1-3件事，按精力排序
2. 给一句思维转换（不要鸡汤，要具体、有画面感、跟用户相关）
3. 语气像朋友聊天，不要像命令
4. 如果用户昨天没完成某件事，温和提及，不要指责
5. 控制在150字以内
```

### 微信小程序订阅消息策略

```
首次使用引导流程:
1. 用户打开小程序 → 看到欢迎页
2. 设置每日提醒时间（早上8:00 / 晚上21:00）
3. 弹出授权 → "允许 Wonderful 每天给你发晨启和晚审消息"
4. 用户勾选"总是允许" → 后续可无限推送
5. 如果用户没勾"总是允许" → 每次只授权一次
   → 在每次晚审结尾引导: "明天还要叫你吗？[要的]"
   → 用户点击 [要的] = 再次授权一次
```

### 数据安全

```
用户数据处理原则:
├── 所有 AI 调用不传用户真实姓名，只用 user_id
├── AI memory 存储在自己的服务器，不传给第三方
├── 小程序只获取必要权限（昵称、头像）
├── 不获取手机号（个人开发者也拿不到）
└── SQLite 数据库定期备份到阿里云 OSS（几毛钱/月）
```

---

## 八、技术风险和应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 小程序审核不过 | 无法上线 | 工具类通过率高；AI功能可能需提供资质，先做模板版不调AI |
| AI API 挂了 | 用户收不到消息 | 双源：GLM主 + DeepSeek备，都挂了用预设模板兜底 |
| ICP备案慢 | 延迟上线 | 提前提交；开发期用测试版不需要备案 |
| 用户量暴增 | 服务器扛不住 | SQLite → PostgreSQL；单机 → 阿里云弹性伸缩 |
| AI 生成不当内容 | 被举报下架 | 加内容安全过滤（阿里云内容安全API免费额度）|
| 微信订阅消息限制 | 通知到达率低 | 引导用户勾"总是允许"；备选钉钉/邮件通知 |

---

## 九、后续可选升级路径

```
当前 MVP (¥23-44/月)
  │
  ├── 用户量到 1000+ → 加 DeepSeek 付费 AI
  │
  ├── 用户量到 5000+ → 服务器升配 + 数据库迁移 PostgreSQL
  │
  ├── 想做 App → Flutter 套壳小程序，共用同一套后端 API
  │
  ├── 想做社区(UGC) → 加内容审核 + 举报系统
  │
  ├── 想变现 → 注册企业 → 接入微信支付 → 会员制/高级AI
  │
  └── 想出海 → PWA + Telegram Bot + Claude API
```

---

## 总结

**起步成本：¥23/月（¥270/年），约等于两杯奶茶。**

```
最小可行产品 =
  微信小程序 (¥30/年认证)
  + 微信云开发 (¥19.9/月)
  + 智谱 GLM-4-Flash (免费AI)
  + 1个.cn域名 (¥1首年)

  总计: ¥270/年

  能实现:
  ✅ 任务卡片管理
  ✅ AI 晨启对话
  ✅ AI 晚审复盘
  ✅ AI 思维转换
  ✅ 金币奖励系统
  ✅ 微信订阅消息推送
  ✅ 分享卡片到朋友圈
```
